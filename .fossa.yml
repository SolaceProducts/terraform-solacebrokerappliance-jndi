version: 3

project:
  locator: SolaceProducts_terraform-solacebrokerappliance-jndi
  id: SolaceProducts_terraform-solacebrokerappliance-jndi
  name: terraform-solacebrokerappliance-jndi
  teams: []
  labels:
    - terraform

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
